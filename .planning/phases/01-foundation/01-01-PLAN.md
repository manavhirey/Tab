---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/dialog.tsx
  - components.json
autonomous: true

must_haves:
  truths:
    - "Next.js dev server starts without errors"
    - "TypeScript compilation succeeds"
    - "Tailwind v4 styles apply correctly"
    - "shadcn/ui components render without errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next"
    - path: "src/app/layout.tsx"
      provides: "Root layout"
      min_lines: 15
    - path: "src/lib/utils.ts"
      provides: "cn() helper function"
      exports: ["cn"]
    - path: "src/components/ui/button.tsx"
      provides: "Button component"
      min_lines: 20
    - path: "src/components/ui/card.tsx"
      provides: "Card component"
      min_lines: 20
    - path: "src/components/ui/input.tsx"
      provides: "Input component"
      min_lines: 10
    - path: "src/components/ui/dialog.tsx"
      provides: "Dialog/Modal component"
      min_lines: 30
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import"
      pattern: "import.*globals\\.css"
    - from: "src/components/ui/*.tsx"
      to: "src/lib/utils.ts"
      via: "cn() import"
      pattern: "import.*cn.*from.*utils"
---

<objective>
Create Next.js 16 project with TypeScript, Tailwind v4, and shadcn/ui component system.

Purpose: Establish the project foundation with all tooling configured correctly for mobile-first development.
Output: Running Next.js project with Button, Card, Input, and Dialog components available.
</objective>

<execution_context>
@/Users/manavhirey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/manavhirey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 16 project with defaults</name>
  <files>
    - package.json
    - tsconfig.json
    - next.config.ts
    - src/app/layout.tsx
    - src/app/page.tsx
    - src/app/globals.css
  </files>
  <action>
Run the following command to create the Next.js project:

```bash
cd /Users/manavhirey/Documents/Code/Tab
npx create-next-app@latest . --yes
```

The `--yes` flag accepts all defaults which includes:
- TypeScript: Yes
- ESLint: Yes
- Tailwind CSS: Yes (v4)
- src/ directory: Yes
- App Router: Yes
- Turbopack: Yes (for dev)
- Import alias: @/*

After creation, verify the project structure matches expected layout with src/app/ directory.

Update src/app/layout.tsx metadata to use the Tab app name:
- title: "Tab - Track IOUs & Subscriptions"
- description: "Simple expense visibility for personal finance"
  </action>
  <verify>
```bash
cd /Users/manavhirey/Documents/Code/Tab && npm run dev
```
Dev server starts on localhost:3000 without errors.
  </verify>
  <done>
Next.js 16 project exists with TypeScript and Tailwind v4 configured. Dev server runs successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and add base components</name>
  <files>
    - components.json
    - src/lib/utils.ts
    - src/components/ui/button.tsx
    - src/components/ui/card.tsx
    - src/components/ui/input.tsx
    - src/components/ui/dialog.tsx
  </files>
  <action>
Initialize shadcn/ui with the following command:

```bash
cd /Users/manavhirey/Documents/Code/Tab
npx shadcn@latest init
```

When prompted, accept defaults:
- Style: New York
- Base color: Neutral
- CSS variables: Yes

This creates:
- components.json (shadcn/ui configuration)
- src/lib/utils.ts (cn() helper using clsx and tailwind-merge)
- Updates to globals.css with CSS variables for theming

Then add the four base components:

```bash
npx shadcn@latest add button card input dialog
```

This installs:
- src/components/ui/button.tsx
- src/components/ui/card.tsx
- src/components/ui/input.tsx
- src/components/ui/dialog.tsx

Also installs dependencies: clsx, tailwind-merge, lucide-react, tw-animate-css

Verify globals.css has the @theme block with color variables (Tailwind v4 CSS-first configuration).
  </action>
  <verify>
```bash
cd /Users/manavhirey/Documents/Code/Tab
cat src/lib/utils.ts | grep "export function cn"
cat src/components/ui/button.tsx | head -5
npm run build
```
utils.ts exports cn(), button.tsx exists, build succeeds.
  </verify>
  <done>
shadcn/ui initialized. Button, Card, Input, and Dialog components available at @/components/ui/*. cn() helper available at @/lib/utils.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Tailwind v4 CSS configuration</name>
  <files>
    - src/app/globals.css
  </files>
  <action>
Verify that globals.css uses Tailwind v4 CSS-first configuration:

1. Check for `@import "tailwindcss"` (not @tailwind directives)
2. Check for `@theme inline` block with CSS custom properties
3. Verify color variables exist: --background, --foreground, --primary, --card, etc.

If shadcn/ui init configured correctly, no changes needed. If using old @tailwind directives, update to v4 format per research document.

Create a simple test: Update src/app/page.tsx to display a Button and Card to verify components work:

```tsx
import { Button } from "@/components/ui/button"
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card"

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-4">
      <Card className="w-full max-w-sm">
        <CardHeader>
          <CardTitle>Tab</CardTitle>
        </CardHeader>
        <CardContent className="space-y-4">
          <p className="text-muted-foreground">
            Track IOUs and subscriptions
          </p>
          <Button className="w-full">Get Started</Button>
        </CardContent>
      </Card>
    </main>
  )
}
```

This serves as a visual test that Tailwind and shadcn/ui are working correctly.
  </action>
  <verify>
```bash
cd /Users/manavhirey/Documents/Code/Tab
npm run dev &
sleep 5
curl -s http://localhost:3000 | grep -q "Tab" && echo "Page renders" || echo "Page failed"
pkill -f "next dev"
```
Page renders with Tab content.
  </verify>
  <done>
Tailwind v4 with CSS-first configuration working. shadcn/ui components render correctly with proper styling.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npm run build` completes successfully
3. Browser shows styled Card with Button at localhost:3000
4. No TypeScript errors in terminal
5. Tailwind classes apply (visible styling on components)
</verification>

<success_criteria>
- Next.js 16 project created with TypeScript and Tailwind v4
- shadcn/ui initialized with New York style
- Four base components available: Button, Card, Input, Dialog
- cn() helper function available at @/lib/utils
- Dev server runs successfully
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
